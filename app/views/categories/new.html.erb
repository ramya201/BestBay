<%= form_for Category.new do |f| %>
    <div class="actions">
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Create new category", :class=>"btn btn-primary" %>
    </div>
<% end %>