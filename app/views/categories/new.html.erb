<%= form_for Category.new do |f| %>
    <div class="actions">
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Add a new category", :class=>"btn btn-primary" %>
    </div>
<% end %>

