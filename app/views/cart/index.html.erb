<h2>Shopping Cart</h2>
<hr/>
<h3>You have <%= @_controller.get_total_item_count.to_s%> items in your cart</h3>
<hr/>
<% total = 0 %>
<table>
  <% @_controller.get_all_items_in_cart.each do |cart_item|%>
      <% item=Item.find(cart_item.fetch("table").fetch("cart_item_id"))%>
      <tr style="background-color: #33CCCC;">
        <th>Image</th>
        <th>Name</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Total</th>
        <th></th>
      </tr>
      <tr>
        <td style="vertical-align: middle; text-align: center;"><%= image_tag(item.image,:alt => "Item Image",:width => "180px",:height => "150px") %> </td>
        <td style="vertical-align: middle; text-align: center;"><%= link_to item.name, item_path(item) %></td>
        <td style="vertical-align: middle; text-align: center;"><%= instant_price=item.instant_price.to_i %></td>
        <td style="vertical-align: middle; text-align: center;"><%= count=cart_item.fetch("table").fetch("count")%></td>
        <td style="vertical-align: middle; text-align: center;"><%= instant_price*count%></td>
        <td style="vertical-align: middle; text-align: center;"><%= link_to "Remove", remove_cart_path(item.id), :class => "btn btn-primary", :method => "get"  %></td>
      </tr>
    <% total = total + instant_price*count %>
  <%end%>
<tr>
  <td></td>
  <td></td>
  <td></td>
  <td></td>
  <td><%= "Total = #{total}" %></td>
</tr>


</table>

